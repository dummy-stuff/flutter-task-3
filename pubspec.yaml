name: users_fetch
description: A simle flutter app which displays data from an API.
version: 1.1.1+5

environment:
  sdk: ">=2.5.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0+4

    
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
